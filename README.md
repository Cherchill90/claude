# Zeitwaage — Arbeitszeit-Erfassung für Lehrkräfte

- **`app/`** — the implemented app (React PWA frontend + Express/Excel sync backend).
  Start here: [`app/README.md`](app/README.md) for setup and run instructions.
- **`design/`** — the original Claude Design handoff bundle (chat transcript + HTML/CSS/JS
  prototypes) this implementation was built from, kept for reference.
